class Product{
    constructer(id, brand, product_type, ingrediants, description, price) {
     this.id = id;     
     this.brand = brand;
     this.product_type = product_type;
     this.ingrediants = ingrediants;
     this.description = description;
     this.price = price;
    }
    
}

